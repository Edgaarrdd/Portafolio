
import { defineCollection, z } from 'astro:content';
import { glob } from 'astro/loaders';

// Archivo de configuración para Colecciones de Contenido.
// Actualmente vacío porque el proyecto no usa colecciones.
export const collections = {

};
